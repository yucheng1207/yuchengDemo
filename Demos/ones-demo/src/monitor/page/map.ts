/* eslint-disable */
const pageMap = {
  '/team/:teamUUID/spaces': '知识库管理-页面组列表',
};

// 检查上述语义化是否存在重复。会持续用到，注释，不移除
// const valueMap = {};
// for (const key in pageMap) {
//   if (valueMap[pageMap[key]]) {
//     console.log(key, pageMap[key]);
//   }
//   if (pageMap[key]) {
//     valueMap[pageMap[key]] = true;
//   }
// }

export { pageMap };
